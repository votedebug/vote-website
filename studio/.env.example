# The URL Presentation tool should preview by default. Defaults to
# http://localhost:5173 (the Vite app running locally) if unset.
# Update this to your Vercel deployment URL once the site is live.
SANITY_STUDIO_PREVIEW_ORIGIN=http://localhost:5173
